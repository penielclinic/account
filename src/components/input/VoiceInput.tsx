'use client';

// Phase 2에서 구현 — 음성 입력 컴포넌트
// 흐름: Web Speech API → 텍스트 변환 → lib/claude/parse-voice.ts → 폼 자동 채움
export default function VoiceInput() {
  return (
    <div className="p-4 border-2 border-dashed border-gray-300 rounded-lg text-center text-gray-400 text-sm">
      음성 입력 — Phase 2에서 구현됩니다
    </div>
  );
}
